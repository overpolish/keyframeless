# Parameter

For any `FULL_WIDTH` parameters the chevron is not visible, but it *is* still there and can be clicked. Hunt around for the chevron, as long no view is blocking the chevron you will see the context menu.