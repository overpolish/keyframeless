build/
.build/
release/
DerivedData/
xcuserdata/
.DS_Store
*.xcuserstate
.clangd
buildServer.json
scripts/*.fcpxml
!scripts/*.template.fcpxml
# Per-product installer artifacts (generated transiently by build-and-sign.sh from
# the combined project + uninstall.template; never committed).
Distribution/*.pkgproj
!Distribution/Keyframeless.pkgproj
Distribution/scripts/uninstall-*
!Distribution/scripts/uninstall-motionblur
!Distribution/scripts/uninstall-legacy-plugins

# Staged kk-ai-helper binary + metallib (built by build-and-sign.sh keyframelessai;
# the plist/uninstaller/postinstall/entitlements alongside them ARE committed source).
Distribution/helper/staging/
.derived-data*
.claude
CLAUDE.md
.compile
.cache
# Vendored transpiler build artifacts (run ThirdParty/build-transpiler.sh)
ThirdParty/build/
